---
phase: 02-sidebar-shortcuts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Sidebar.tsx
  - src/app/dashboard/page.tsx
  - src/app/dashboard/orders/page.tsx
  - src/app/dashboard/chat/page.tsx
  - src/app/dashboard/profile/page.tsx
autonomous: true

must_haves:
  truths:
    - "Sidebar displays navigation shortcuts for all user roles"
    - "Users can access Orders, Chat, Profile from sidebar without going through dashboard tabs"
    - "Navigation links work on mobile and desktop"
  artifacts:
    - path: "src/components/Sidebar.tsx"
      provides: "Sidebar with navigation shortcuts"
      min_lines: 160
    - path: "src/app/dashboard/orders/page.tsx"
      provides: "Dedicated orders page"
    - path: "src/app/dashboard/chat/page.tsx"
      provides: "Dedicated chat page"
    - path: "src/app/dashboard/profile/page.tsx"
      provides: "Dedicated profile page"
  key_links:
    - from: "Sidebar.tsx"
      to: "/dashboard/orders"
      via: "next/link"
    - from: "Sidebar.tsx"
      to: "/dashboard/chat"
      via: "next/link"
    - from: "Sidebar.tsx"
      to: "/dashboard/profile"
      via: "next/link"
---

<objective>
Add sidebar navigation shortcuts for profile, settings, orders, and chat. Move these from dashboard tabs to the main sidebar for quick access from any page.

Purpose: Improve navigation by making all shortcuts accessible from the persistent sidebar
Output: Updated sidebar with shortcuts, new dashboard sub-pages
</objective>

<execution_context>
@/home/jawad/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/jawad/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md — Tech stack: Next.js + TypeScript + Tailwind CSS + Supabase
@.planning/1-CONTEXT.md — Previous phase context (Phase 1 was implementation)
@.planning/codebase/STRUCTURE.md — Project structure

**Current sidebar (src/components/Sidebar.tsx):**
- Home (/)
- Dashboard (/dashboard)
- Admin (/admin) - for admin/seller roles
- Login/Sign Out

**Current dashboard tabs:**
- Orders - order history and status
- Chat - real-time messaging
- Profile - user info display
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add navigation shortcuts to Sidebar</name>
  <files>src/components/Sidebar.tsx</files>
  <read_first>src/components/Sidebar.tsx</read_first>
  <action>
Update Sidebar component to include these navigation shortcuts:

1. Add new imports for additional icons (Package for Orders, MessageCircle for Chat)
2. Add new userLinks array with:
   - { href: '/dashboard/orders', label: 'My Orders', icon: Package }
   - { href: '/dashboard/chat', label: 'Chat', icon: MessageCircle }
   - { href: '/dashboard/profile', label: 'Profile', icon: User }
   - { href: '/dashboard', label: 'Dashboard', icon: ShoppingBag } (keep as is)

3. Keep existing admin links and conditional rendering based on user role

The sidebar should maintain the existing structure: logo section, user info section, navigation links, and bottom actions (sign out).
  </action>
  <verify>
<automated>grep -c "My Orders\|/dashboard/orders" src/components/Sidebar.tsx</automated>
  </verify>
  <done>Sidebar shows My Orders, Chat, Profile shortcuts visible in sidebar</done>
</task>

<task type="auto">
  <name>Task 2: Create dedicated orders page</name>
  <files>src/app/dashboard/orders/page.tsx</files>
  <read_first>src/app/dashboard/page.tsx (lines 167-243 for orders tab)</read_first>
  <action>
Create a new dedicated orders page at src/app/dashboard/orders/page.tsx:

1. Copy the orders tab content from the dashboard page
2. Use the same layout with the sidebar (will inherit from root layout)
3. Ensure it uses the same supabase queries for fetching orders
4. Include the status update functionality for admin/seller roles
5. Maintain the same mobile-first responsive design

This creates a standalone /dashboard/orders route accessible from sidebar.
  </action>
  <verify>
<automated>test -f src/app/dashboard/orders/page.tsx && grep -q "orders" src/app/dashboard/orders/page.tsx</automated>
  </verify>
  <done>Dedicated orders page accessible at /dashboard/orders</done>
</task>

<task type="auto">
  <name>Task 3: Create dedicated chat page</name>
  <files>src/app/dashboard/chat/page.tsx</files>
  <read_first>src/app/dashboard/page.tsx (lines 245-302 for chat tab)</read_first>
  <action>
Create a new dedicated chat page at src/app/dashboard/chat/page.tsx:

1. Copy the chat tab content from the dashboard page
2. Include the real-time message fetching and sending functionality
3. Include auto-refresh every 10 seconds
4. Keep the message input at the bottom

This creates a standalone /dashboard/chat route accessible from sidebar.
  </action>
  <verify>
<automated>test -f src/app/dashboard/chat/page.tsx && grep -q "messages" src/app/dashboard/chat/page.tsx</automated>
  </verify>
  <done>Dedicated chat page accessible at /dashboard/chat</done>
</task>

<task type="auto">
  <name>Task 4: Create dedicated profile page</name>
  <files>src/app/dashboard/profile/page.tsx</files>
  <read_first>src/app/dashboard/page.tsx (lines 304-338 for profile tab)</read_first>
  <action>
Create a new dedicated profile page at src/app/dashboard/profile/page.tsx:

1. Copy the profile tab content from the dashboard page
2. Display user information: name, email, phone, address, city
3. Include sign out button

This creates a standalone /dashboard/profile route accessible from sidebar.
  </action>
  <verify>
<automated>test -f src/app/dashboard/profile/page.tsx && grep -q "Profile" src/app/dashboard/profile/page.tsx</automated>
  </verify>
  <done>Dedicated profile page accessible at /dashboard/profile</done>
</task>

</tasks>

<verification>
- [ ] Build passes: npm run build
- [ ] Sidebar displays all new navigation links
- [ ] Clicking sidebar links navigates to correct pages
- [ ] Orders page shows order history
- [ ] Chat page shows messages and allows sending
- [ ] Profile page shows user information
</verification>

<success_criteria>
Sidebar navigation shortcuts are accessible from any page. Users can click on Orders, Chat, or Profile in the sidebar to navigate directly to those sections without going through the dashboard tabs first.
</success_criteria>

<output>
After completion, create `.planning/phases/02-sidebar-shortcuts/02-01-SUMMARY.md`
</output>