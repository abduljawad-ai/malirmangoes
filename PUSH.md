# Commands to push to GitHub

cd /home/jawad/Desktop/mango-store

# Then run autopush alias (just like before):
autopush

# Or if that doesn't work, do manually:
git add .
git commit -m "feat: new mango store with admin panel"
git push